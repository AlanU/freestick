#ifndef ANALOGAXISWIDGET_H
#define ANALOGAXISWIDGET_H

#include <QWidget>

class AnalogAxisWidget : public QWidget
{
    Q_OBJECT
public:
    explicit AnalogAxisWidget(QWidget *parent = 0);
    ~AnalogAxisWidget();

signals:

public slots:
};

#endif // ANALOGAXISWIDGET_H
